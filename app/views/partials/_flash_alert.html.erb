<% flash.each do |key, value| %>
  <div id="flash-alert" data-alert class="alert-box round">
    <div class="alert alert-<%= key %>">
      <%= value %>
    </div>
    <a href="#" class="close">&times;</a>
  </div>
<% end %>