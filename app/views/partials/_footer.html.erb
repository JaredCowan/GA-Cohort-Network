<div id="footer">
  <footer>
    <div class="panel">
      <%= debug(params) if Rails.env.development? %>
      <%#= logger.inspect if Rails.env.development? %>
      <%#= controller.inspect if Rails.env.development? %>
      <br />
      <code>
        <samp>Find me in partials/footer</samp>
      </code>
    </div>
  </footer>
</div>